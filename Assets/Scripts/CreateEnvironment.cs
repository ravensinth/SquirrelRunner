using UnityEngine;
using System.Collections;

public class CreateEnvironment : MonoBehaviour {
    /*
    public GameObject Tree = null;
    public GameObject LastTreeElement = null;
    // Use this for initialization
    void Start() {
        InvokeRepeating("CreateTree", 1f, 1.5f);
    }

    // Update is called once per frame
    void Update() {

    }

    void CreatTree() {
        Instantiate(Tree, Tree.transform.position, Quaternion.identity);
    }
     */
}
